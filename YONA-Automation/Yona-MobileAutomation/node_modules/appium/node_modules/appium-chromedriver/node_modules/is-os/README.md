Just convenience methods for Node `os` module.

`npm install is-os`

```
var isWindows = require('is-os').isWindows;
isWindows() // false
```

Functions:
 - isWindows()
 - isLinux()
 - isMac()
 - is64bit()
 - is32bit()
